<section>
  Media Spotlight: TODO LINK NAME OF WORK WITH TOP VOTES
  <br /> TO DO Show # Votes - category
</section>

<section>
  <div>
    <h3>Top Movies</h3>
    <ul>
      <% @works_movies.each do |movie| %>
      <li>
        <%= link_to movie.title, work_path(movie.id) %> by <%= movie.creator %> <br /> TODO Show # votes
      </li>
      <% end %>
    </ul>
  </div>
  <div>
    <h3>Top Books</h3>
    <ul>
      <ul>
        <% @works_books.each do |book| %>
        <li>
          <%= link_to book.title, work_path(book.id) %> by <%= book.creator %> <br /> TODO Show # votes
        </li>
        <% end %>
      </ul>
    </ul>
  </div>
  <div>
    <h3>Top Albums</h3>
    <ul>
      <ul>
        <% @works_albums.each do |album| %>
        <li>
          <%= link_to album.title, work_path(album.id) %> by <%= album.creator %> <br /> TODO Show # votes
        </li>
        <% end %>
      </ul>
    </ul>
  </div>
</section>
